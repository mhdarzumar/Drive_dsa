{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile and Run",
            "type": "shell",
            "command": "javac \"${fileDirname}/contest.java\" && java -cp \"${fileDirname}\" contest < \"${fileDirname}/input.txt\" > \"${fileDirname}/output.txt\"",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Compiles and runs the Java program with input redirection"
        }
    ]
}
